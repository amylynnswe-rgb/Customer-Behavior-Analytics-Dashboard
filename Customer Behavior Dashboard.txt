1. Replace null values in review rating with median, group by category. (mean is influenced by the outliers)

2. Replace columns' names with lowercases and underscores. It's easier to read and less likely to get errors.

3.purchase_amount_(usd) is changed to just purchase_amount

4. create a new column named 'age_group'. q cuts groups into equal sizes and labels as defined.

5. create a column 'purchase_frequency_days' by mapping with frequency_of_purchases

6. Check if promo code used and discount applied are the same

7. install and connect to sqlite
 
Now We move on to SQL
Q1. What is the total revenue generated by male vs. female customers?
select gender, SUM(purchase_amount) as revenue
from customer
group by gender

Male	157890
Female	75191
--Q2. Which customers used a discount but still spent more than average amount?
Select customer_id, purchase_amount
from customer
where discount_applied = 'Yes' and purchase_amount >= (select AVG(purchase_amount) from customer)

--Q3. Which are the top 5 products with the highest review ratings?
select top 5 item_purchased, AVG(review_rating) as "Average Product Rating"
from customer
group by item_purchased 
order by avg(review_rating) desc

Gloves	3.86
Sandals	3.84
Boots	3.82
Hat	3.8
Skirt	3.78

--Q4. Compare the average purchase amounts between standard and express shipping.
select shipping_type, round(avg(purchase_amount), 2) 
from customer
where shipping_type in ('Standard', 'Express')
group by shipping_type
Express	60.48
Standard	58.46

--Q5. Do subscribed customers spend more? Compare average spend and total revenue between total revenue between subscribers and non-subscribers
select subscription_status,
count(customer_id) as total_customers,
round(avg(purchase_amount), 2) as average_spend,
round(sum(purchase_amount), 2) as total_revenue
from customer
group by subscription_status
order by total_revenue, average_spend desc

Yes	1053	59.49	62645
No	2847	59.87	170436

--Q6. Which products have the highest percentage of purchases with discount applied?
select top 5
item_purchased, round(100*sum(case when discount_applied = 'Yes' then 1 else 0 end)/count(*), 2) as discount_rate
from customer
group by item_purchased
order by discount_rate desc
Hat	50
Coat	49
Sneakers	49
Sweater	48
Pants	47

--Q7. Segment customers into new, returning, and loyal based on their total o of prev purchases, and show the count of each segment.
with customer_type as (
select customer_id, previous_purchases, 
case when previous_purchases = 1 then 'New'
when previous_purchases between 2 and 10 then 'Returning'
else 'loyal'
end as customer_segment
from customer)

select customer_segment, count(*) as no_of_customers
from customer_type
group by customer_segment

Returning	701
loyal	3116
New	83

--Q8. Which products are top 3 most purchased within each category?
with item_counts as (
select category, item_purchased, count(customer_id) as total_orders,
row_number() over(partition by category order by count(customer_id) desc) as item_rank
from customer
group by category, item_purchased)

select item_rank, category, item_purchased, total_orders
from item_counts
where item_rank<=3

--Q9. Are customers who are repeat buyers (more than 5 prev purchases) also likely to subscribe?
select subscription_status, count(customer_id) as repeat_buyers
from customer
where previous_purchases>5
group by subscription_status

Yes	958
No	2518

--Q10. revenue by age_group
select age_group, sum(purchase_amount) as total_revenue
from customer
group by age_group
order by total_revenue desc

Young Adult	62143
Middle Age	59197
Adult	55978
Senior	55763

Finally, we connect database to power BI to start dashboard. 
-New measures created for 3 cards: no of customers, avg purchase amount, avg review rating
-donut chart for subscription status in legend and count of customer_id in values
-clustered column chart Y-purchase amount, X-category
-another clustered column chart Y-no of customer, X-Category
-add button slicers for subscription status, gender, category
-add list slicer for shipping type
-clustered bar chart for revenue by age group and sales by age group





